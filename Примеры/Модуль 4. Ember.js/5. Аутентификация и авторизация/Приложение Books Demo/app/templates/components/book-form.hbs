<form onsubmit={{action "saveBook"}}>
  <div class="field">
    <label>Title:</label>
    {{input type="text" placeholder="Title" value=title}}
  </div>

  <div class="field">
    <label>ISBN:</label>
    {{input type="text" placeholder="ISBN" value=isbn}}
  </div>

  <div class="field">
    <label>Publish Date:</label>
    {{input type="date" placeholder="Publish Date" value=publishDate}}
  </div>

  <div class="field">
    <label>Author:</label>
    <p class="control">
      {{#power-select searchEnabled=true onchange=(action (mut author)) selected=author search=(action "searchAuthor") as |author|}}
        {{author.lastName}} {{author.firstName}}
      {{/power-select}}
    </p>
  </div>

  <div class="field">
    <label>Don't see Your author?</label>
    {{new-author-modal onSave=(action (mut author))}}
  </div>
  <div class="form-footer">
    <button type="submit" class="btn-submit">save</button>
  </div>
</form>